# Claude task: independent far-end accent on every printer rope

Adi's new request: "about 10 led from the end of the led - opposite of where wire connects - those should just be pure white all the time, actually not just white give me option to select a color / turn on rainbow mode for those led, and then rest can behave the way we were planning".

You remain the lead implementer on this Pi. Implement this feature and its UI/tests directly in /home/edi/printer-light-studio. Codex coordinates and verifies. Read LIVE_ROLLBACK_NOTICE.md FIRST: your previous report's deployment section is now stale. The live host source was restored to the original smooth renderer after Adi reported that Codex's provisional renderer looked terrible. The experimental Python renderer and UI are NOT active on the physical lights. A plain service restart must not be described as activating your build now. Keep your changes preview-only; no host edits, service restarts, broker/printer commands or firmware changes. Correct the stale deployment instructions in README/CLAUDE_BUILD_REPORT.md and your new report.

## Required behavior

- Each of the seven 100-position ropes has two independently composed zones: 90 positions for printer state and animation, and the last 10 addressable positions at the far end opposite the wire for an accent. This is ten addressable positions, not ten of the three LED dies within each WS2811 module.
- Default accent: solid neutral white, all R/G/B equal, with no state tinting or breathing. Give a per-rope solid-color picker and a rainbow mode, plus an easy Apply to all ropes action. Include an independent accent brightness control if useful; keep the established physical RGB cap and do not change firmware brightness. No off mode is required.
- The accent's physical location is fixed relative to the wire. Assuming physical pixel 0 is at the data-in wire, reserve physical indices 90–99. The existing reverse-direction option changes only the 90-position status region's direction. It must NEVER move the accent to the wire end. Make wire-end versus accent-end clear in the installation UI and preview, including when direction is reversed.
- Recompute progress across the 90-position status region: 0%, 50%, 100% map to that region alone. The cap is not included in the print percentage. Events, error/paused/offline/unknown states, completion celebrations and explicit Identify must not overwrite the accent zone. Identify should mark the status region while preserving the cap. Always honor the existing global output-suspension semantics and physical cap; do not introduce an independent competing publisher.
- Rainbow should be a smooth, bounded animation in the reserved section. Design it for the real protocol budget: a uniform cycling hue across the ten positions is a good low-cost default, or tiered spatial colors if your measured budget supports it. A full rainbow gradient that consumes the entire message allowance is not acceptable. Keep static accents static and coalesce updates. Do not promise physical smoothness from a browser preview.
- Persist accent settings with the layout; migrate existing schema-1/2 layouts to default white caps without losing mapping, names, direction, other settings or undo. Validate counts, color channels (reject bool/NaN/invalid values), modes and unknown keys. Support the fixed default 10; do not add arbitrary segment editing unless needed for this request.
- In Map the wall and Animation lab, show the zone boundary, status versus accent, and color/rainbow controls clearly. Maintain the single draft/Save & apply workflow. Browser preview must use the actual Python compositor with the same reverse/zone rules as physical output, without any hardware writes.

## Verification and small review follow-ups

Run the allowed hardware-free command `python3 -m unittest discover -s tests -v`. Add meaningful regressions for cap location before/after reverse; every printer state and Identify preserving the selected accent; solid white channels equal; rainbow changes with time within budget; correct 90-position percentage; migration/save/undo; per-rope versus all-rope settings; invalid settings; browser/API field compatibility. Update tests/browser_check.py so Codex can run the new UI workflow with its local Playwright runtime.

While making this change, check two concrete issues from Codex's read-through of the earlier version (they may already have changed in your final pass):
1. _printing previously used a fixed 2-second cycle but travel/30 could exceed 2 seconds at low percentages. Verify a droplet actually reaches the waterline and splashes at low progress, not just resets mid-flight. Fix with a regression if still present.
2. observed_complete was persisted without job association and could survive a disconnected interval into a different retained finished job. Do not show an old completion age as belonging to a new job. Suppress stale association on a changed job or uncertain interval; job names are not reliable unique IDs, so be conservative.

Do not independently activate the new code. Adi needs a hardware-free preview, then a separately approved bounded physical trial comparing smoothness with the original renderer before replacing the current live lighting.

Deliver the implementation, tests, updated editing/deployment documentation and CLAUDE_ACCENT_REPORT.md. Clearly separate executed tests from unverified browser/hardware behavior. Update only your ## Claude section in AGENTS.md. Preserve runtime data, backups, original references and all unrelated projects.
