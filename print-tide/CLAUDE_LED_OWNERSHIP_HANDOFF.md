# Claude owns the Pi printer-light implementation

Adi's latest instruction: "bro you have to get most of the work done by claude on the pi". You are the lead implementer. Codex will coordinate, independently verify, and handle narrowly scoped live installation. Do substantive implementation here, not just a review or a list of recommendations.

Original request: "Can you get the Claude on a Raspberry Pi to make a visualizer for me, so that I can map the right LEDs to the right printers? Because when I'm putting them up, I don't want to mismatch them. And if I mismatch them, I just want to rearrange them visually through HTML UI or something. It also needs to come up with a lot more things related to the printer. Like it's right now just showing the print percentages, like if there's an error, orange, like breathing bars. If nothing is running, what's the state? Maybe like it should be bluish, watery, and there should be constant animation, state animations. And since there's seven of them, like literally side by side, maybe there's some other animations, like maybe every animation is a ripple effect in all of them as well. So all of those things, can you plan that and get Raspberry Pi to make a UI for that as well? And what else can we do with this setup?"

## Current state, verified by Codex

Your earlier dispatch hit the session limit before implementation. Codex built a provisional version in /home/edi/printer-light-studio and installed it at http://100.65.17.33:8772/. Adi corrected this division of work: you must now own the design and implementation. Use the baseline where helpful, but use your judgment to substantially improve and finish the actual product. Do not preserve poor design simply because Codex wrote it.

Read README.md, DESIGN.md, AGENTS.md and reference/LED_STUDIO_BRIEF.md, plus the source. Your prior local Pi context is valuable. Correct assumptions when your project evidence is better. Current standalone package is light_studio; static assets are served directly from this directory. Changes to HTML/CSS/JS appear on browser reload; loaded Python requires a later restart coordinated by Codex. Work only inside this project and preserve reference/ copies, runtime data/ and deployment backups/receipts. Do not edit credentials, existing host source, services, firmware, or any other project.

Live host: /home/edi/printer-led-mcp/server.py, currently integrated with light_studio. Existing printer-led-mcp user service owns both telemetry and lighting. Original source snapshot is reference/server.py; installed integration is server.staged.py generated by integrate.py. Integration selects the UI's Tailscale address with resolve_bind_host("auto"), independently of MCP_HOST (which is 0.0.0.0 in the original service env). Keep this fix.

Current map: printer1→node03, printer2→node04, printer3→node02, printer4→node05, printer5→node06, printer6→node07, printer7→node08. Every rope is 100 pixels. node01 is unrelated: never target it. Physical positions and printer model mapping are unverified. At the last post-install check all seven printers had fresh telemetry, six RUNNING and printer7 FINISH. All seven nodes retained online and each produced successful ACKs during a four-second passive check; none sent error ACKs. This is timestamped evidence, not perpetual liveness.

22 core/HTTP tests passed on the Pi. Codex also tested the demo and live browser: seven bays, menu and drag swaps, saving/reloading, undo, physical ordering, direction edit, Identify restoration, walkthrough, moving preview, eight simulated states, settings persistence, mobile width and no JS errors. These do not make the provisional implementation beyond criticism.

## Your implementation task

Own a thorough product pass against Adi's actual request. Read the code, identify shortcomings, and implement the improvements you judge most valuable. Prioritize a reliable installation workflow and expressive, useful animations. In particular, the provisional renderer is crude broad bands at 2 Hz, the animation lab forces the same simulated state on every bay, and field/connection semantics, Identify behavior, live-versus-draft clarity and settings interactions deserve scrutiny. Improve these materially, not just cosmetic text. A per-printer mixed-state playground, better water/drop/ripple choreography under a measured transport budget, and a clearer guided mapping workflow are good candidates, but your design judgment leads.

Consider useful richer printer states only when supported by current snapshots. Do not invent error causes, job IDs, model assignments, bed clearance or AMS data. A retained FINISH is not a new completion or evidence of an empty bed. Include a concise roadmap of additional things this setup can do, distinguishing implemented features from future ones.

Keep one physical writer. Browser simulation must never publish. Physical Identify is explicitly user-triggered, bounded, and automatically restores current state even if the browser disappears. Keep duplicate/unknown-node rejection, revision conflicts, persistent atomic mapping and collection acknowledgment, and backward-compatible host interfaces or provide an exact staged integration change. Existing cast disable must promptly suspend hardware output. No printer commands, firmware edits, service restarts, package/global installs, or Discord actions in this run. Render output must obey existing strict fill/range/pixel/off firmware payloads. Respect RGB cap and finite aggregate message budget; coalesce old frames and keep each rope receiving updates fairly. Keep error/pause/offline distinguishable during effects.

You have file editing tools confined to this workspace and may run only the exact test command `python3 -m unittest discover -s tests -v`. Tests must be hardware-free. Do not make network calls or execute hardware/service modules through tests. Write meaningful regressions for changes you make. Codex will handle additional browser tests and any required staged-source installation. Do not ask to defer implementation; the user already authorized you to build this.

Update your own ## Claude section in AGENTS.md; leave ## Codex intact. Write CLAUDE_BUILD_REPORT.md with your design decisions, implemented changes, test evidence, exact touched files, limitations, and whether the host integration needs replacement or only a restart. Return after implementing your substantive pass, not after merely planning it. Clearly attribute the baseline to Codex and your changes to yourself.
