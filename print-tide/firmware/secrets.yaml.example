# Copy to secrets.yaml (mode 600). Values must match what the nodes were flashed
# with on 2026-08-25 from the Mac — OTA is refused if ota_password differs.
wifi_ssid: "Device-Northwestern"
wifi_password: "CHANGE_ME"
mqtt_broker: "10.106.4.15"      # Pi mosquitto LAN IP (see /home/edi/ledwall-mcp/ledwall.conf for user/pw)
mqtt_username: "CHANGE_ME"
mqtt_password: "CHANGE_ME"
ota_password: "CHANGE_ME"
